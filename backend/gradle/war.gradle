apply plugin: "war"

bootWar {
    mainClassName = "fr.cesi.cube.GoodfoodApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
